# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual Environment
venv/
.venv/
env/
ENV/

# Distribution
dist/
build/
*.egg-info/
*.egg

# Environment (JANGAN commit .env berisi secret/API key)
.env
.env.local
.env.*
!.env.example

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Testing
.pytest_cache/
htmlcov/
.coverage
.coverage.*

# Type checking
.mypy_cache/
.pytype/

# Model files (large, should be stored in LFS or external storage)
models/*.keras
models/*.h5
models/*.pkl
models/*.model
models/*.bin
models/*.npy
artifacts/**/models/
*.keras
*.model
tokenizer_*.pkl

# Generated datasets (besar; kontraknya split_contract.json + skrip rebuild)
artifacts/bert/input/*.csv
# Hasil HPC: hanya BOBOL/LOG BESAR yang di-ignore; file kecil
# (probs_*.csv, roc_data, training_log, threshold) WAJIB di-track
# karena paper/make_figs.py membacanya.
artifacts/bert/results/model/
artifacts/bert/results/checkpoints/
artifacts/bert/results/*.bin
artifacts/bert/results_realonly/

# Training outputs: PNG regenerable di-ignore; CSV audit/triage HARUS
# di-track (input paper + jejak audit).
artifacts/bilstm/output/*.png

# OCR output (generated, not source)
ocr_output/

# Dataset images (large files)
dataset/*.jpg
dataset/*.jpeg
dataset/*.png

# Training outputs (legacy pola; pola artefak spesifik sudah di atas)
classification_report_*.csv
predictions_test_*.csv

# Logs
*.log

# TensorFlow
*.tflite
saved_models/
checkpoints/

# Hasil HPC (500MB+, simpan di storage/HPC, bukan git)
